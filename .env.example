# Cloudinary Configuration
# Required for AI Lens image upload feature
# Get these values from your Cloudinary dashboard: https://cloudinary.com/console

VITE_CLOUDINARY_CLOUD_NAME=your-cloud-name-here
VITE_CLOUDINARY_UPLOAD_PRESET=your-upload-preset-name-here

# Instructions:
# 1. Copy this file to .env: cp .env.example .env
# 2. Replace the placeholder values with your actual Cloudinary credentials
# 3. Create an upload preset in Cloudinary dashboard if you haven't already
#    - Settings > Upload > Upload presets > Add upload preset
#    - Set signing mode to "Unsigned" for client-side uploads
